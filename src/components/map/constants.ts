// Shared map constants — kept outside SFCs so they can be re-exported.
export const MOWER_LENGTH_M = 0.55
export const DOCK_PADDING_M = 0.45
export const DOCK_SIZE_M = MOWER_LENGTH_M + DOCK_PADDING_M
